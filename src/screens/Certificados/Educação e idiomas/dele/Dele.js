import Layout from '../../../Container/Containerr.js'
import { View, Text, TouchableOpacity, FlatList } from 'react-native';



const NavegaDele = () => {

    return (
        <Layout Title={'Dele'}>
        </Layout>
        
    );
};

export default NavegaDele; // Exportação correta do componente
