import Layout from '../../../Container/Containerr.js'
import { View, Text, TouchableOpacity, FlatList } from 'react-native';



const NavegaToefl = () => {

    return (
        <Layout Title={'Toefl'}>
            
        </Layout>
        
    );
};

export default NavegaToefl; // Exportação correta do componente