import Layout from '../../../Container/Containerr.js'
import { View, Text, TouchableOpacity, FlatList } from 'react-native';



const NavegaEnem = () => {

    return (
        <Layout Title={'Enem'}>
        </Layout>
        
    );
};

export default NavegaEnem; // Exportação correta do componente

